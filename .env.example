# Database
DB_NAME=taskdb
DB_USER=flaskuser
DB_PASSWORD=change-me-strong-password
MYSQL_ROOT_PASSWORD=change-me-root-password

# Flask
FLASK_DEBUG=0
SECRET_KEY=change-me-run-python-secrets-token-hex-32

# AWS (reference only — not used by docker-compose)
AWS_REGION=us-east-1
